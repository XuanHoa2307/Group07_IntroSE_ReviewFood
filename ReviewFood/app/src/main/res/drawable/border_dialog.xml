<!-- res/drawable/border_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="10dp" /> <!-- Điều này là tùy chọn, làm cho góc tròn -->
    <solid android:color="#EFEAEA" /> <!-- Màu nền của layout -->
    <stroke
        android:width="0dp"
        android:color="#000000" /> <!-- Màu của đường viền -->
</shape>
